// import { decodeQuote } from "./quote";

// export function getCollateral(
//   buffer: Buffer,
//   pccsUrl: string = "https://api.trustedservices.intel.com/"
// ) {
//   const quote = decodeQuote(buffer);
//   const pccs = new Pccs(pccsUrl);
//   const collateral = await pccs.getCollateral(quote);
//   return collateral;
// }
